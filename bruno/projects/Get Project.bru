meta {
  name: Get Project
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/projects/{{projectId}}
  body: none
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{projectId}}
}
